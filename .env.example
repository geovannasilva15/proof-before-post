# Server-only credential used by the analyze and translate API routes. Never expose it with NEXT_PUBLIC_.
OPENAI_API_KEY=

# Optional. The default is the model recommended for new web-search integrations.
OPENAI_MODEL=gpt-5.6
