# Server-only credential used by app/api/analyze. Never expose it with NEXT_PUBLIC_.
OPENAI_API_KEY=

# Optional. The default is the model recommended for new web-search integrations.
OPENAI_MODEL=gpt-5.5
